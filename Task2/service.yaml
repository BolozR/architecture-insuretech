apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
  namespace: sprint8
spec:
  type: NodePort
  selector:
    app: scaletestapp
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
